# models directory
# Trained model artifacts (.pkl) are excluded from git (see .gitignore).
# JSON evaluation files (evaluation_results.json, training_metadata.json, etc.) ARE committed.
# See README § Model Artifacts for download instructions if you need the .pkl files.
